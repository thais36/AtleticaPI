<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Consultar Modalidades</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            text-align: center; /* Centraliza o texto */
            padding: 10px; /* Adiciona um pouco de espaçamento */
        }
        th {
            background-color: #94210a;
            color: white;
        }
        .button {
            padding: 5px 10px;
            margin: 0 5px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .edit-button {
            background-color: blue;
            color: white;
        }
        .delete-button {
            background-color: red;
            color: white;
        }
    </style>
</head>
<body style="font-family: Arial, sans-serif; margin: 0; padding: 0; background-color: #FFFFE0;">
    <header class="header" style="background-color: #94210a; padding: 20px; text-align: center; color: white;">
        <h1>Gestão do Sistema</h1>
    </header>

    <nav class="navbar" style="display: flex; justify-content: space-between; align-items: center; padding: 10px 20px; background-color: #f1f1f1; color: #333;">
        <ul class="navbar-menu" style="list-style: none; display: flex; gap: 20px; margin: 0; padding: 0;">
            <li><a href="/" style="color: #333; text-decoration: none;">Home</a></li>
            <li><a href="/consultarInscricao" style="color: #333; text-decoration: none;">Membros Inscritos</a></li>
            <li><a href="/consultarContato" style="color: #333; text-decoration: none;">Recados</a></li>
            <li><a href="/consultarModalidade" style="color: #333; text-decoration: none;">Modalidades Esportivas</a></li>
            <li><a href="/consultarEvento" style="color: #333; text-decoration: none;">Eventos</a></li>
            <li><a href="/consultarLoja" style="color: #333; text-decoration: none;">Produtos</a></li>
        </ul>
        <a href="/cadastrarModalidade" style="padding: 10px 20px; background-color: green; color: white; text-decoration: none; border-radius: 5px;">Adicionar Modalidade</a>
    </nav>

    <div class="container mt-4" style="padding: 20px;">
        <h4>Consulta de Modalidades Esportivas</h4>
        <p>Lista das modalidades disponíveis na Atlética</p>
        
        <table border="1">
            <thead>
                <tr>
                    <th>Nome</th>
                    <th>Descrição</th>
                    <th>Dias</th>
                    <th>Horário</th>
                    <th>Local</th>
                    <th>Ações</th> <!-- Coluna para ações -->
                </tr>
            </thead>
            <tbody>
                {{#if modalidades.length}}
                    {{#each modalidades}}
                    <tr>
                        <td>{{this.modalidade}}</td>
                        <td>{{this.descricao}}</td>
                        <td>{{this.dias}}</td>
                        <td>{{this.horario}}</td>
                        <td>{{this.local}}</td>
                        <td>
                            <a href="/editarModalidade/{{this.id}}" class="button edit-button">Editar</a>
                            <form action="/excluirModalidade/{{this.id}}" method="POST" style="display:inline;">
                                <button type="submit" class="button delete-button">Excluir</button>
                            </form>
                        </td>
                    </tr>
                    {{/each}}
                {{else}}
                    <tr><td colspan="6" style="text-align: center;">Nenhuma modalidade encontrada.</td></tr>
                {{/if}}
            </tbody>
        </table>
    </div>

</body>
</html>
