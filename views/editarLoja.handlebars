<!-- editarLoja.handlebars -->

<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestão do Sistema - Editar Produto da Loja</title>
    <style>
        body {
            font-family: Arial, sans-serif; 
            margin: 0; 
            padding: 0; 
            background-color: #FFFFE0;
        }

        .header {
            background-color: #94210a; 
            padding: 20px; 
            text-align: center; 
            color: white;
        }

        .navbar {
            display: flex; 
            justify-content: space-between; 
            align-items: center; 
            padding: 10px 20px; 
            background-color: #f1f1f1; 
            color: #333;
        }

        .navbar-menu {
            list-style: none; 
            display: flex; 
            gap: 20px; 
            margin: 0; 
            padding: 0;
        }

        .container {
            padding: 20px; 
            max-width: 600px; 
            margin: 20px auto;
            border: 1px solid #ddd; 
            border-radius: 5px; 
            box-shadow: 2px 2px 8px rgba(0,0,0,0.1);
            background-color: white;
        }

        label {
            margin-top: 5px; 
           
            display: block;
        }

        input[type="text"],
        textarea {
            width: 100%;
            padding: 10px;
            margin-top: 3px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }
        
        button {
            background-color: #94210a; 
            color: white; 
            padding: 10px 15px; 
            border: none; 
            border-radius: 4px; 
            cursor: pointer;
        }

        button:hover {
            background-color: #7b1d0c;
        }

        .button-container {
            text-align: center; 
            margin-top: 15px;
        }
    </style>
</head>

<body>
    <header class="header">
        <h1>Gestão do Sistema -Editar Produto da Loja</h1>
    </header>
       

<nav class="navbar" style="display: flex; justify-content: space-between; align-items: center; padding: 10px 100px; background-color: #f1f1f1; color: #333;">
    <ul class="navbar-menu" style="list-style: none; display: flex; gap: 40px; margin: 0; padding: 0; width: 100%; justify-content: space-between;">
        <li><a href="/consultarInscricao" style="color: #333; text-decoration: none;" class="navbar-link">Membros Inscritos</a></li>
        <li><a href="/consultarContato" style="color: #333; text-decoration: none;" class="navbar-link">Recados</a></li>
        <li><a href="/consultarModalidade" style="color: #333; text-decoration: none;" class="navbar-link">Modalidades Esportivas</a></li>
        <li><a href="/consultarEvento" style="color: #333; text-decoration: none;" class="navbar-link">Eventos e Campeonatos</a></li>
        <li><a href="/consultarLoja" style="color: #333; text-decoration: none;" class="navbar-link">Produtos da Loja</a></li>
    </ul>
</nav>

       <div class="container">
        <h4>Editar Produto da Loja</h4>
        <form action="/atualizarLoja/{{loja.id}}" method="POST">
            <input type="hidden" name="id" value="{{loja.id}}">

                   <div class="container">

        <div>
            <label for="loja" class="form-label">Nome do Produto:</label>
            <input type="text" name="loja" id="loja" value="{{loja.loja}}" placeholder="Ex: Camiseta" required>
        </div>
        <div>
            <label for="descricao">Descrição:</label>
            <textarea name="descricao" id="descricao" placeholder="Ex: Descreva o produto." required>{{loja.descricao}}</textarea>
        </div>
        <div>
            <label for="preco">Preço (R$):</label>
            <input type="text" name="preco" id="preco" value="{{loja.preco}}" placeholder="Ex: 25.00" required>
        </div>


               <div class="button-container">
                <form action="/deletarLoja/{{loja.id}}" method="POST"
                    onsubmit="return confirm('Tem certeza que deseja excluir este Produto?');">
                    <button type="submit">Atualizar</button>
                    <button type="submit">Cancelar</button>
                </form>
            </div>
        </form>
    </div>
    
</body>
</html>