<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Atlética Fatec Zona Leste</title>
    <style>
        /* Estilos gerais */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #FFFFE0;
        }

        /* Estilo do cabeçalho */
        .header {
            background-color: #94210a;
            padding: 20px;
            text-align: center;
            color: white;
        }

        .header img {
            max-width: 150px;
            height: auto;
        }

        /* Botão de inscrição */
        .inscricao-button {
            background-color: #01480a;
            color: white;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 5px;
        }

        /* Estilos para o container de cards */
        .card-container {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            justify-content: center;
            padding: 20px;
        }

        /* Estilos para cada card */
        .card {
            background-color: #f9f9f9;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            width: 250px;
            padding: 15px;
            text-align: center;
            color: #333;
        }

        /* Estilos para título do card */
        .card-title {
            font-size: 1.2em;
            color: #94210a;
            margin-bottom: 10px;
        }

        /* Estilos para o botão de ações */
        .card-action {
            margin-top: 15px;
            padding: 8px 12px;
            background-color: #01480a;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
        }

        /* Estilo do rodapé */
        .footer {
            padding: 20px;
            background-color: #f1f1f1;
            color: #333;
            text-align: center;
        }

        .footer a {
            color: #94210a;
            text-decoration: none;
        }

        /* Estilos para o chatbot */
        #chat-widget {
            position: fixed;
            bottom: 20px;
            right: 20px;
            width: 350px;
            background-color: #080808;
            border: 1px solid #ccc;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            font-size: 14px;
            display: flex;
            flex-direction: column;
        }

        #chat-output {
            flex-grow: 1;
            max-height: 300px;
            overflow-y: auto;
            padding: 10px;
            border-bottom: 1px solid #ccc;
            background-color: #f9f9f9;
            border-radius: 8px;
            margin-bottom: 10px;
        }

        #chat-input {
            width: calc(100% - 70px);
            padding: 10px;
            border: none;
            outline: none;
            border-radius: 5px;
            font-size: 14px;
            margin: 20px;
        }

        #chat-send {
            background-color: #01480a;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 14px;
            text-align: center;
            width: 60px;
            margin-left: 80%;
        }

        #chat-send:hover {
            background-color: #013d07;
        }
    </style>
</head>

<body>

    <!-- Header -->
    <header class="header">
        <img src="/images/icon/logo.png" alt="Atlética Fatec Zona Leste">
        <h1>Atlética Fatec Zona Leste</h1>
        <p>Fique por dentro do que acontece na Atlética Fatec Zona Leste!</p>
    </header>

<!-- Chatbot -->
<div id="chat-widget">
    <div id="chat-output"></div>
    <input id="chat-input" type="text" placeholder="Digite sua mensagem..." />
    <button id="chat-send">Enviar</button>
    <button id="chat-clear">Limpar</button>
</div>

<script>
    document.getElementById('chat-send').addEventListener('click', async () => {
        const inputElement = document.getElementById('chat-input');
        const input = inputElement.value;
        if (input.trim() === '') return; // Não envia se o campo estiver vazio

        // Mostra a mensagem do usuário no chat
        document.getElementById('chat-output').innerHTML += `<div style="color: blue; font-weight: bold;">Você: ${input}</div>`;

        // Envia a mensagem para a rota '/chatbot'
        const response = await fetch('/chatbot', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ message: input }),
        });

        const data = await response.json();
        if (data.reply) {
            document.getElementById('chat-output').innerHTML += `<div style="color: red; font-weight: bold;">Bot: ${data.reply}</div>`;
        } else {
            document.getElementById('chat-output').innerHTML += `<div style="color: red; font-weight: bold;">Bot: Desculpe, não entendi sua mensagem. Poderia refazê-la, por favor?</div>`;
        }

        // Limpa o campo de entrada após o envio
        inputElement.value = '';
    });

    // Limpar as respostas do chat
    document.getElementById('chat-clear').addEventListener('click', () => {
        document.getElementById('chat-output').innerHTML = '';
    });
</script>


    <!-- Botão de Inscrição -->
    <div style="text-align: center; margin: 20px;">
        <a href="/inscricao" class="inscricao-button">Inscreva-se na Atlética</a>
    </div>

    <!-- Modalidades em formato de Cards -->
    <section class="modalidades">
        <h2 style="text-align: center; color: #94210a;">Modalidades Disponíveis</h2>
        <div class="card-container">
            {{#if modalidades.length}}
            {{#each modalidades}}
            <div class="card">
                <h3 class="card-title">{{this.modalidade}}</h3>
                <p><strong>Descrição:</strong> {{this.descricao}}</p>
                <p><strong>Dias:</strong> {{this.dias}}</p>
                <p><strong>Horário:</strong> {{this.horario}}</p>
                <p><strong>Local:</strong> {{this.local}}</p>
            </div>
            {{/each}}
            {{else}}
            <p style="text-align: center;">Nenhuma modalidade encontrada.</p>
            {{/if}}
        </div>
    </section>

    <!-- Mapa e Localização -->
    <section class="mapa" style="padding: 20px; background-color: #f9f9f9; text-align: center;">
        <h2 style="color: #94210a; margin-bottom: 10px;">Mapa da Localização</h2>
        <iframe
            src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3658.277058255061!2d-46.478550424671134!3d-23.522535478826544!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x94ce61270808f47f%3A0xddc2deca4778eb69!2sFATEC%20Zona%20Leste!5e0!3m2!1spt-BR!2sbr!4v1730691709709!5m2!1spt-BR!2sbr"
            width="80%" height="300" style="border:0; max-width: 500px;" allowfullscreen="" loading="lazy"
            referrerpolicy="no-referrer-when-downgrade"></iframe>
    </section>

    <section class="localizacao" style="padding: 15px; background-color: #f9f9f9; text-align: center;">
        <h3 style="margin-bottom: 10px; color: #94210a;">Localização</h3>
        <p style="margin: 0; color: #333; font-size: 14px;">
            A FATEC Zona Leste está localizada na Avenida Águia de Haia, 2983 - Cidade A. E. Carvalho, São Paulo - SP,
            CEP: 03694-0001.
            <br>
            Para mais informações, visite o site oficial:
            <a href="https://fateczl.cps.sp.gov.br/" target="_blank">FATEC Zona Leste</a>
        </p>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <p>Atlética Fatec Zona Leste © 2024</p>
        <div class="footer-social">
            <a href="https://www.instagram.com/atleticafateczl/" target="_blank" rel="noopener noreferrer">
                <i class="fab fa-instagram" style="font-size: 30px; margin-left: 10px;"></i> Instagram da Atlética
            </a>
        </div>
    </footer>

</body>

</html>