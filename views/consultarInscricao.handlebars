<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Consulta de Inscrições - Atlética Fatec Zona Leste</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #FFFFE0;
        }
        .header {
            background-color: #94210a;
            color: white;
            padding: 20px;
            text-align: center;
        }
        .navbar {
            margin-bottom: 20px;
        }
        .table-responsive {
            margin-top: 20px;
        }
    </style>
</head>
<body>

    <!-- Header -->
    <header class="header">
        <h1>Gestão de Inscrições</h1>
    </header>

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">SISTEMA WEB</a>
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/consultarInscricao">Membros Inscritos</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/consultarContato">Recados</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/consultarModalidade">Modalidades Esportivas</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/consultarEvento">Eventos</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/consultarLoja">Produtos</a>
                    </li>
                </ul>
                <a class="btn btn-success" href="/cadastrarInscricao">Adicionar Inscrição</a>
            </div>
        </div>
    </nav>

    <!-- Form Title -->
    <div class="container">
        <h4>Consulta de Inscrições</h4>
        <p>Lista de inscritos na Atlética Fatec Zona Leste</p>

        <div class="table-responsive">
            <table class="table table-bordered">
                <thead class="table-primary text-white">
                    <tr>
                        <th scope="col" class="text-center">Nome</th>
                        <th scope="col" class="text-center">Endereço</th>
                        <th scope="col" class="text-center">CEP</th>
                        <th scope="col" class="text-center">Celular</th>
                        <th scope="col" class="text-center">Ações</th>
                    </tr>
                </thead>
                <tbody>
                    {{#if posts.length}}
                        {{#each posts}}
                            <tr>
                                <td class="text-center">{{nome}}</td>
                                <td class="text-center">{{endereco}}</td>
                                <td class="text-center">{{cep}}</td>
                                <td class="text-center">{{celular}}</td>
                                <td class="text-center">
                                    <a href="/editarInscricao/{{id}}" class="btn btn-sm btn-warning me-1">Editar</a>
                                    <form action="/excluirInscricao/{{id}}" method="POST" style="display:inline;">
                                        <button type="submit" class="btn btn-sm btn-danger">Excluir</button>
                                    </form>
                                </td>
                            </tr>
                        {{/each}}
                    {{else}}
                        <tr>
                            <td colspan="5" class="text-center">Nenhuma inscrição encontrada.</td>
                        </tr>
                    {{/if}}
                </tbody>
            </table>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/js/bootstrap.min.js"></script>
</body>
</html>
