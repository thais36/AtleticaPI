<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Consulta de Inscrições - Atlética Fatec Zona Leste</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css" rel="stylesheet">
   
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #FFFFE0;
        }
        header {
            background-color: #94210a;
            padding: 20px;
            text-align: center;
            color: white;
        }
        .navbar {
            display: flex; 
            justify-content: center; 
            align-items: center; 
            padding: 10px 20px; 
            background-color: #f1f1f1; 
            color: #333;
        }
        .navbar-menu {
            list-style: none;
            display: flex;
            gap: 40px;
            margin: 0;
            padding: 0;
        }
        .navbar-menu li a {
            color: #333;
            text-decoration: none;
        }
        .container {
            padding: 20px;
            max-width: 800px;
            margin: 20px auto;
            background-color: white;
            border: 1px solid #ddd;
            border-radius: 5px;
            box-shadow: 2px 2px 8px rgba(0,0,0,0.1);
        }
        .add-button {
            padding: 10px 20px; 
            background-color: green; 
            color: white; 
            text-decoration: none; 
            border-radius: 5px;
            margin-bottom: 10px;
            display: inline-block;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            padding: 10px;
            border: 1px solid #ddd;
            text-align: center;
        }
        th {
            background-color: #94210a;
            color: white;
        }
        .button {
            padding: 5px 10px; 
            margin: 0 5px; 
            border: none; 
            border-radius: 5px; 
            cursor: pointer;
        }
        .edit-button {
            background-color: blue; 
            color: white;
        }
        .delete-button {
            background-color: red; 
            color: white;
        }
    </style>

</head>
<body>

    <header class="header">
        <h1>Gestão de Inscrições - Membros da FATEC Zona Leste</h1>
    </header>

    <!-- Navbar -->
    <nav class="navbar">
        <ul class="navbar-menu">
            <li><a href="/consultarInscricao">Membros Inscritos</a></li>
            <li><a href="/consultarContato">Recados</a></li>
            <li><a href="/consultarModalidade">Modalidades Esportivas</a></li>
            <li><a href="/consultarEvento">Eventos e Campeonatos</a></li>
            <li><a href="/consultarLoja">Produtos da Loja</a></li>
        </ul>
    </nav>

    <div class="container">
        <h4>Consulta de Inscrições</h4>
        <div style="text-align: right;">
            <a href="/inscricao" class="button add-button">Inscrever Membro</a>
        </div>
    

        <p>Lista de inscritos na Atlética Fatec Zona Leste</p>

        <table>
            <thead>
                <tr>
                    <th>Nome</th>
                    <th>Curso</th>
                    <th>Semestre</th>
                    <th>Turno</th>
                    <th>Email</th>
                    <th>Celular</th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody>
                {{#each inscricoes}}
                    <tr>
                        <td>{{inscricao}}</td>
                        <td>{{curso}}</td>
                        <td>{{periodo}}</td>
                        <td>{{turno}}</td>
                        <td>{{email}}</td>
                        <td>{{celular}}</td>
                        <td>
                            <a href="/editarInscricao/{{this.id}}" class="button edit-button">Editar</a>
                            <form action="/deletarInscricao/{{this.id}}" method="POST" style="display:inline;">
                                <button type="submit" class="button delete-button" onclick="return confirm('Deseja excluir esta inscrição?')">Excluir</button>
                            </form>
                        </td>
                    </tr>
                {{else}}
                    <tr>
                        <td colspan="7">Nenhuma inscrição encontrada.</td>
                    </tr>
                {{/each}}
            </tbody>
        </table>
    </div>

</body>
</html>
